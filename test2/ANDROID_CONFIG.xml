<!-- Add this to android/app/src/main/AndroidManifest.xml -->
<!-- Add these permissions inside the <manifest> tag -->

<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<!-- Add this to your <application> tag for Google Sign-in configuration -->
<activity
    android:name="com.google.android.gms.auth.api.signin.internal.SignInHubActivity"
    android:theme="@android:style/Theme.Translucent_NoTitleBar" />
